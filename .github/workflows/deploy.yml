name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main          # adjust to your default branch if different (e.g. master)
  workflow_dispatch:  # allow manual runs from the Actions tab

# Grant GITHUB_TOKEN the permissions needed to deploy to Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment; cancel in-progress runs
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build static site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # If your repo is deployed at username.github.io/repo-name (not a custom domain),
      # set NEXT_PUBLIC_BASE_PATH in your repo's Settings → Secrets and variables → Actions.
      # Example value: /repo-name
      # Leave the secret unset (or empty) for custom domains or root-level pages sites.
      - name: Build and export
        run: pnpm build
        env:
          NEXT_PUBLIC_BASE_PATH: ${{ vars.NEXT_PUBLIC_BASE_PATH }}

      # next.config.mjs writes output to ./out when output: "export" is set
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
